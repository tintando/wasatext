# Media shot list

One file, referenced once from `README.md`.

| File | In frame | Why it exists |
|---|---|---|
| `hero.png` | The chat view: conversation list on the left with unread badges and photo and forwarded previews, a group conversation open on the right, a reply quoting a photo message, a reaction, and two messages from the signed-in account showing `✓✓` (read by every recipient) and `✓` (delivered, one recipient has not opened it yet). | The first thing anyone sees. The two receipts side by side are the project's argument in one glance, and they are the part of it a still image can carry. |

The photo message itself sits just above the top of the frame. Its caption survives inside the quote in the reply, which is enough to show that photos, captions and replies exist without giving a picture of a picture the top third of the hero.
